export default function machinePage({params}){
    return (
        <div>
            <h1>Machine ID: {params.id}</h1>
            {/* You can add more details or components related to the machine here */}
        </div>
    )
}